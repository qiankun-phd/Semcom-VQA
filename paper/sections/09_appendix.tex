% RETIRED FILE (2026-08-06) --- not \input by main.tex.
% The reproducibility record and the TOST equivalence details now live in
% supplementary.tex (S1/S2), with IMAGE-CLUSTERED statistics per the
% revision (review-panel-2026-08.md P0-2): pooled primary-pool
% delta*=0.0028; second pool equivalence NOT established at delta=0.01
% (clustered delta*=0.0158). The pre-revision, cluster-naive TOST table
% that used to live here (including the post-hoc "equivalence within
% +/-0.011" wording) has been deleted so it cannot be reintroduced by
% accident. Do not \input this file.
