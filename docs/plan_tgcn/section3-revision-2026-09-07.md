# 第三节修改记录与待核实项

## 本轮范围

重写系统模型，提供中文对照，并同步修正相邻章节的命名、算法执行顺序及直接冲突的计量说明。未修改实验原始数据、图表数值或摘要中的性能数字。主稿不再使用数字编号表示两条证据分支；正文使用 detection evidence / image，公式使用 det / img。F13 绘图脚本仅修改两项图例命名，并用原数据重新生成 PDF、PNG、SVG；主稿引用的 PDF 与 PNG 已同步。

## 已完成的正文调整

- 第三节组织为网络与查询、证据与回答、信道与传输、能耗与时延四部分。
- 数据集、划分、评分、校准与数值参数移至实验设置。
- 移除未被公式支持的跨相干块时间分集论证，以及固定 0.8–1 dB / 1.3 倍有限码长保证。
- 将信息中断近似、固定码率 LDPC FER 和图像理想化速率核算分开说明，不再宣称三者构成已验证的统一物理层实现。
- 区分紧凑数据包主计量与满窗口预算计算，不再称主结果使用满窗口保守端。
- 能耗分为无人机与边缘两端，显式计入选择器相关的检测成本。第四节伪代码同步采用按需检测。
- 补充查询已知、查询传递及解析不计入、每问题独立计量、无视频闭环验证等范围假设。
- 保留评估集计数校准的事实，删除“相同校准必然不会导致方法间偏差”的断言。

## 本地核查依据

1. `code/vqa_semcom_v0/recovery/build_comparison_v2.py`：`SE_TOKEN = 0.5`；`uses_digital()` 对检测证据按比特数除以 0.5 核算。图像按记录载荷除以遍历频谱效率核算。
2. `paper/data/w5_persample_energy.json`：检测器参考值为 0.4275 J，发射功率为 0.5 W。14 ms 对应 0.4345 J；0.3 s 对应 0.5775 J（均为 η=1 的预算计算）。
3. `code/vqa_semcom_v0/recovery/build_latency_breakdown.py`：上传、机载处理与回答推理时延相加，不存在“传输占满 0.3 s 而总时延仍不超过 0.3 s”的计量。
4. `code/vqa_semcom_v0/recovery/digital_link.py`：本地恢复版为固定码率 LDPC FER 模块；损坏模型按记录处理，丢弃/损坏各半。不能将此恢复版当作当前自适应主实验的完整实现。
5. `docs/plan_tgcn/w2-energy-perimeter-2026-08.md`：Jetson 能耗范围是声明的模型，不是机载实测。

## 尚不能通过文字修改解决

### 独立校准主结果

补充材料报告了训练集独立拟合的计数检查，但当前主表仍来自使用评估集校准的记录。正式替换主表需要恢复原始检测记录、校准脚本和各路由器训练/评估流程，训练集拟合后固定参数，重新生成全部关联比较与区间。不能把现有 0.451→0.448 的局部检查写成全流程已重新验证。

### 自适应物理层与图像策略

恢复脚本导入 `vqa_semcom.degradation.digital_link`，但本地 `code/vqa_semcom_v0/src/vqa_semcom/degradation/` 目录不存在。现有 `recovery/digital_link.py` 不包含该脚本所需的 `ergodic_spectral_efficiency` 等自适应实现。

需要恢复实验版本并核实：

- 损失作用单位究竟是整包还是目标记录，中断阈值采用哪一种长度；
- JPEG 字节预算、质量搜索、下采样和灰图替代的完整规则；
- 衰落相关时间、实际编码/交织/重传方式与 FER—空口时间的联合关系；
- 有限码长及实际码率（构造的 LDPC 维度可能不同于标称 1/2）。

本轮没有通过编造缺失函数或参数填补上述空缺。当前第三节描述评估抽象，不能据此宣布已经完成链路级可部署验证。

另需核对恢复版 build_comparison_v2.py 的数据划分条件：learn_policy() 在 is_test(image_id) == split_test 时跳过样本，而调用传入 split_test=False。这与“仅用训练集拟合”的注释相反。应追溯生成正式结果的脚本版本和调用参数，不能据此直接断言现有全部结果使用了错误划分，也不能在复核之前将该恢复脚本作为独立训练的证明。本轮未运行或修改此恢复脚本。

### 后续实验叙述与图

- 现有 F12 图中的码率、零计算能耗及能耗示例仍是简化图示；本轮图注已限定核算口径，未重画图片。
- F13 的预算可调路由柱在既有脚本中按 (1 - 图像比例) 分摊检测器成本，这与逐样本路由每次均运行检测器的模型不一致；本轮只改图例，未重新分解该柱或改变数据。后续应核对总能耗是否已含全查询检测成本，再修正各组件分解，不能把遗漏部分误记为无线能耗。
- 原有总体节能、统计等价及全 SNR 排序等结论仍需随独立校准和物理层审计一并复核。
- 当前修改解决的是结构与可确认的计量矛盾，不代表所有实验风险已消除。

## 编译与检查

主稿、中文第三节与补充材料均通过 latexmk 编译；检查交叉引用、引用及公式，未发现未定义引用或 Overfull 报错。英文第三节及相邻改动页、中文全部页面和补充材料校准说明页已渲染检查。未执行新的推理或实验重跑。

## 回退副本

本轮修改前的第三节、第四节、实验节与补充材料保存在 `/tmp/hppo-section3-before-9F9o1a/`。该目录属于临时存储，不替代长期版本管理。
