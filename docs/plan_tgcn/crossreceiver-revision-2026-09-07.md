# 跨接收器匹配子集：冻结协议

用户于 2026-09-07 授权子代理完成后续跨接收器工作。此工作仅使用已有缓存与 CPU 小型路由训练；原数据、主实验、补充实验与论文 TeX 均不修改，不启动 VLM、检测器或新图像编码。

## 比较问题与数据门限

- 接收器为 Qwen2-VL-2B-Instruct、Qwen2.5-VL-3B-Instruct、HuggingFaceTB/SmolVLM-Instruct。身份同时检查配置（存在时）和每条图像分支模型字段。
- 使用 v3_0_rician 与 v25/v26 的 main/cmp/extra 缓存，先求三者完全一致的 `(image, question, SNR)` 双分支公共键，然后固定同一训练、验证、测试集合。
- 分割保留 crc32(image)%100：<20 测试，>=80 验证，其余训练。不能重新划分以改变结果。
- 严格去重：metadata、预测、正确性、字节、证据字符串、模型、检测器、置信阈值和接收图路径必须一致；只忽略运行时延与 freshness 副本字段。冲突则停止。
- 跨接收器要求 GT、问题、类别、原始/接收检测计数、检测证据字符串及非 counting 检测结果一致；接收 JPEG 逐文件 SHA-256 必须一致，路径名可以不同。
- 初查公共键 14,604（训练 9,150，验证 2,646，测试 2,808）。默认与替代缓存的旧计数校准不同：594 条校准值、181 条正确性差异。只允许这些旧 counting 校准派生字段不同；使用公共训练数据拟合唯一新校准器并替换三者 counting 标签，随后断言三者检测标签完全一致。不能混用旧检测标签。
- 必须保存每问题类型、每划分的图像和决策数量。此公共集不同类型覆盖不均，不称为完整五类型等量基准。额外完整五类型共同图像子集只作为敏感性报告，不能替换主公共键结果。

## 固定训练与迁移

- 沿用主实验18维安全输入白名单，无答案 polarity/风险/视角标注等。仅公共训练集拟合计数校准与 LUT。
- 三个接收器分别拟合双分支 MLP，seed 0–9，隐藏层32/16、Adam及全部优化器参数沿用原 fit_pair，最多300 epoch，仅图像级验证集 BCE 早停。共30次双分支拟合，不搜索超参数。
- 基线：全图像、全检测、类型规则、训练集 LUT、固定400步线性路由；oracle仅准确率。
- 主评估 lambda=0，指标为准确率和图像分支使用率，分别给出实际公共键池化、每 SNR、每类型以及完整五类型图像子集结果。保存全部种子，不挑选最佳种子。
- 另加冻结源接收器迁移：本轮相同公共键上训练的 Qwen2 路由概率与选择原封不动迁移到两个目标接收器，只用目标标签评分；目标数据不参与源模型训练、选参或重新校准。明确与接收器特定重新拟合区分，不称后者为零样本泛化。
- 无可靠替代接收器的功耗实测，因此不报告跨接收器真实能耗或节能率，不做价格扫描。默认 Qwen2 的32.31 J不能视为其他接收器测量。

## 执行和保存

- 主机 lab-s2，RA_DI Python；CUDA禁用、BLAS单线程、nice10。
- 单元测试和真实公共数据3接收器各seed0、2epoch冒烟通过后，才启动正式30次拟合。
- 新建输出 crossreceiver_smoke_v1 / crossreceiver_v1，禁止覆盖或静默恢复。
- 保存脚本、配置和数据哈希、全部公共键/覆盖、接收 JPEG 哈希、共享校准、模型、早停曲线、标签、验证/测试概率与逐样本选择、source-frozen选择、状态和日志。
- 首轮 summary 为全部种子的均值及样本标准差，是描述性结果。缓存图像非独立场景、任务覆盖不均限制泛化；不把训练种子当独立测试样本。图像聚类配对检验留给统一结果分析，不因30次训练完成而宣称统计显著。

## 启动证据

- 6项回归测试通过；真实全量缓存审计与3接收器各2epoch冒烟通过，crossreceiver_smoke_v1/status.json=COMPLETE。
- 正式后台会话 `tgcn-crossreceiver-20260907`，PID4162046，启动时间UNIX1788798001.621487。
- 正式脚本SHA256：`3d493b212a490650f7419aca7a38616cabd7583e21950cc3397c628dee44ccde`；原训练脚本SHA256：`ea2d2b905981676611c11bf7a2fa826c863fa283d92f66a6bfc1b0a010ade494`。
- 日志：`/home/qiankun/phd_research/vqa_semcom/outputs/revision_20260907_independent/crossreceiver_v1.log`。
- 启动预计2–5分钟，不保证完成时间；以status.json为准。正式数据尚未产生时不报告冒烟性能。

## V1安全停止：发送端特征来源新增门限

父代理通知跨数据集审计发现部分额外类型的 raw_detector_count 可能实际记录接收后计数。即使三个接收器此字段一致，也不能证明它是发送前可见信息。因此已核对精确命令后停止本任务PID4162046，保留全部 v1 文件及日志并添加 INVALIDATION.json=INVALIDATED_PENDING_SENDER_AUDIT。v1不能作为已验证正式结果。

后续必须从原始检测框缓存恢复同图同类别发送端count，核对SNR不变性，报告被修正字段数量；新实验采用新的版本目录，不覆盖v1。

## V2协议增补：原始检测框恢复

- 已定位实际代码原因：run_v1_detector_eval.py 对comparison/co_presence/threshold把接收后计数写入cache tuple中的raw_detector_count位置；不是发送前特征。计数任务不受该写入错误影响。
- 从v2_0_snr_detections.csv恢复Rician发送端count；逐图比较九份相关检测缓存的框签名Counter（image_id/category/bbox/confidence/detector_model，排除latency），并逐决策验证各任务源缓存计数覆盖。presence/counting日志作为sender anchor，必须与原框完全一致。
- 只替换输入g.raw（原始count与nonzero特征），保留g['1']['raw']以及transmitted等原记录不变。发送端count由同图同类别原框确定，不随SNR或接收器改变。
- 只读审计主formal输入发现：AWGN0/29370键；Rayleigh341/29370（train214/validation57/test70）；Rician44/29370（train28/validation6/test10）字段不一致。均发生在三个额外类型。未重跑、改写formal或supplement输出。
- 新正式输出改为crossreceiver_v2。V1永久保留作已中止历史，不用作新结果。9项回归测试覆盖恢复后保留原日志、sender anchor拒绝错误和SNR不变性。
- 由于count特征截断为60，实际18维输入改变数量小于原字段差异数：Rayleigh279键（train166/validation54/test59），Rician33键（train17/validation6/test10）。跨接收器三者各44个原字段恢复，其中33个实际输入改变。

## V2正式启动证据

最终crossreceiver_smoke_v3使用同一正式脚本哈希，真实数据审计和3次双分支2epoch训练全部通过。

- 会话：`tgcn-crossreceiver-v2-20260907`；PID4167814；启动UNIX1788798236.0375922。
- 正式SHA256：`7d749d67eec5318229e4ea642a925c117bf5b315c669d1653addba2f95e49b6e`。
- 正式日志：`outputs/revision_20260907_independent/crossreceiver_v2.log`。
- 已通过正式全量门限并进入训练，进程nice10、约100%CPU，无GPU调用。

## V2完成与完整性检查

正式状态COMPLETE，30/30双分支拟合完成，总耗时73.553秒（包含缓存与接收JPEG审计）。日志恰好30条完成记录，无Traceback/Warning/Error。

后检查读取全部30个已存模型重新计算验证和测试概率，逐数组与保存概率一致；重新计算所有默认选择、各汇总结果及source-frozen目标评分，全部一致。NPZ数组有限，ZIP CRC通过；所有早停曲线长度与记录相符且不超过300epoch；源预测未发生目标接收器重新拟合或重新选择。

108个正式文件已同步本地 `paper/outputs/revision_20260907_independent/crossreceiver_v2`，按排序的相对路径与逐文件SHA256构造总清单哈希，两端一致：`b8ec2266d6e5de580fb6b27d95812729312d0b437f6b5292f5869c3923d4d941`。

### 覆盖边界

共同键14604；训练/验证/测试决策9150/2646/2808，图像343/101/104。测试presence624键、counting312键各只来自26图像；comparison/co_presence/threshold各624键各来自104图像。主池化结果因此不同于原完整问题分布，不能拿其数值直接对比原全量结果。

完整五类型共同图像共131张（训练82/验证23/测试26）；测试敏感性子集1404决策，保存在每个方法记录中，不替换主结果。

### 完整10种子描述性结果

准确率单位为%，±为种子间样本标准差；默认lambda=0。

| 接收器 | 接收器特定MLP拟合 | 冻结本轮Qwen2源MLP | 线性路由 | 类型规则 | 全图像 |
|---|---:|---:|---:|---:|---:|
| Qwen2-VL-2B | 69.626±0.376 | 同左 | 69.587 | 69.338 | 62.927 |
| Qwen2.5-VL-3B | 68.622±0.284 | 68.280±0.432 | 68.376 | 67.308 | 62.571 |
| SmolVLM | 69.291±0.567 | 67.094±0.901 | 68.127 | 67.379 | 59.295 |

MLP图像分支使用率依次28.586%、37.585%、23.636%；冻结源模型对所有接收器相同，为28.586%。共同检测分支准确率67.094%。完整五类型测试图像子集MLP均值依次67.692%、67.016%、67.144%，再次说明任务混合影响池化值。

以上仅描述性比较，不宣称显著性、任意接收器零样本鲁棒性或真实跨接收器节能优势。未进行额外VLM/检测器推理；未修改论文TeX、formal_v1或supplement_v1。原主数据sender特征错误的后续修订由主代理统一处理。
