# Response to Reviewers — Draft Skeleton (2026-08-06)

> Built against the simulated panel (`review-panel-2026-08.md`). When real TGCN reviews arrive, map their comments onto these prepared responses; every response cites the revised manuscript location and the data file behind it. Format: **[Seat-Issue] Comment summary → Response → Change**.

## To all reviewers (summary of changes)

1. **Statistics recast (R1-1/2, DA-cherry-2).** All paired inference is now image-clustered (bootstrap over unique test images, B=5000). The primary-pool rule-vs-LUT TOST passes at the a-priori δ=0.01 (pooled δ*=0.0028); **on the second pool we now state plainly that equivalence is not established at δ=0.01** (clustered δ*=0.0158). The MLP is evaluated over ten fixed seeds (min–max reported). [§IV-B, §VI-J, Supp. S2; `w13_cluster_stats.json`, `w14_mlp_10seed.*`]
2. **Energy accounting recast (R1-4, R3-1/2/4, R2-1).** Eq. (1) now declares radiated power with a PA-efficiency band η∈{0.25,0.4,1}; worst-link dominance margins are restated honestly (12×→6.3×→3.9×) and the system-level ratios shown to be η-invariant. The account is platform-separated (UAV battery vs. edge grid), the idle baseline is treated by sensitivity (a 12 W bare idle would *raise* the incremental VLM term to ≈56 J), and the detector is billed per-query under learned routers. Total-power ratio (≈2.3×) appears in the abstract. [§III-C, §VI-B, §VII-B; `w15_ledger.json`]
3. **Workload-mix dependence (DA-CRITICAL-1, EIC-5).** New mix-sensitivity analysis: presence-share sweep (3.5×→2.8× MLP ratio) and an image-forced-fraction expression (≈1.6× at f=0.5, →1× as f→1); all headline claims rescoped to *structured aerial VQA*. [§VI mix-sensitivity ¶, abstract, §I, §VIII; `w12_wt_pertype.json`]
4. **Positioning repair (R2-3, DA-4).** The type-level pair (rule/LUT) is restored as the deployable contribution; the learned routers are presented as measured headroom under exact offline replay, resolving the tension with the closed-loop observation in Limitation (vi). "Method of record" phrasing removed throughout. [§I, §IV, §VI, §VIII]
5. **Token-PHY consistency (R2-2).** The reviewer is right that the outage model and the airtime billing used different assumptions. §III-D now declares the slot-spreading design (r_min=b/(Bτ)≈0.024 bit/use, matching the reported 0.7%/5.2% outage) and bills token airtime as a range (compact 1.4×10⁴ uses at high SNR ↔ one slot 3×10⁵ in deep fade); the 265× and 49× figures are restated as ranges (≥12×; 14–49×). Energy and routing conclusions are unchanged (the token path remains detector-dominated). [§III-D, §III-G, §VI-N]
6. **Protocol disclosure (R1-3/6).** Train-split-only fitting is now stated for every retrained router (per-receiver and second-pool variants included — this matches the implementation); the MLP's early stopping monitors an internal validation fraction carved from the training split; architecture/optimizer defaults were fixed before test evaluation; seeds fixed in advance. [§IV-B, §VI-L]
7. **Companion-study numbers (EIC-4).** Deleted; the safety bridge is now qualitative with scope stated. [§VII-A, §VII-C(vi)]
8. **Theory (R2-4).** Lemma 2's retention property is now a named assumption (Definition 1, *detectability-preserving coding*) with the honest note that it fails for symbolic types at aggressive compression; Lemma 1 is explicitly labeled near-definitional; proofs moved to supplementary. Prop. 1 retained as a conditional statement over type-measurable policies. [§V, Supp. S4]
9. **Missing references (R2).** Added and integrated: Strinati & Barbarossa (2021), Shao et al. (JSAC 2022), Neurosurgeon (ASPLOS 2017), Kountouris & Pappas (2021), Qin et al. (arXiv 2201.01389) — all metadata verified against primary sources. [§I, §II; refs.bib]
10. **Scope/claim-site hygiene (EIC-2/3, R3-5, DA-5).** Single-tenant + proxy-hardware qualifiers now travel with the headline claims (abstract, §I, §VIII); §VII-B gains quantization and prefill-reuse erosion axes; "structural, not receiver-specific" softened to the tested ≤3B regime; abstract cut ~340→~265 words.

## Prepared per-seat responses (selected)

- **[R3-3] Single VLM energy point across payload range** → We agree this is the main un-run measurement. Response: images are *not* fixed-resolution (dynamic tokenization), so per-item energy may vary with SNR; we scope the compute term to the measured 5 dB workload and can provide per-SNR VLM energy points at the extremes on request (measurement rig unchanged; ~1 day). Parked per revision policy; the 5 dB-workload/dynamic-resolution caveat is now stated in §VII-C(vii).
- **[R3-2] 52.3 W idle** → Explained as the model-resident card (weights loaded, persistence on); sensitivity added showing a lower bare idle *strengthens* dominance. Wall-meter cross-check offered on request.
- **[R3-6] Mission-level context** → Declined for the main text (page limit); we note the latency–hover coupling in fact favors the token path and can add a queries-per-Wh row on request.
- **[DA-6] Cascade baseline** → Not added in this cycle (deployable-first repositioning reduces its centrality); the anti-calibration result (AUC 0.406) blocks the VLM-confidence variant; a detector-abstention cascade is noted as future work. Will add if reviewers insist (replay-computable).
- **Failed attacks acknowledged by the panel's own devil's advocate** (usable verbatim): symbolic-decoder artifact (who-decodes + paraphrase probe), mock numbers (none), broad cherry-picking (disclosure rate), train/test leakage (crc32 split), CSI fragility (mismatch matrix).

## Verification pointers

Every number in the responses traces to `paper/data/w{12,13,14,15}_*.{json,csv}` (replay-computed 2026-08-05/06, scripts on 182 in `~/phd_research/w5_persample_energy/scripts/`).
