# VQA Algorithm Claim Scope

Overall: **claim-scope-ready**
Main claim scope: `architecture_and_executable_fast_layer_not_final_slow_uav_learning`

## Checks

| Check | Status | Detail |
|---|---|---|
| `fast_layer_executable` | `pass` | QRS, tabular contextual bandit, and neural contextual bandit artifacts exist. |
| `radar_to_control_interface` | `pass` | LSS-HSR-L calibration and semantic-control feature adapter exist. |
| `slow_uav_claim_boundary` | `pass` | Docs explicitly prevent claiming final learned UAV slow control. |
| `self_owned_decomposition` | `pass` | Docs frame the method as decomposition plus typed semantic bridge. |

## Allowed Claims

- Role-specific UAV/UE semantic-control architecture is the main algorithmic contribution.
- The executable slice supports deterministic role-split QRS and fast UE/query contextual selectors.
- Typed semantic price and risk guard are the cross-layer algorithm interface.
- LSS-HSR-L radar semantics can be injected through calibration and semantic-control features after data audit.

## Disallowed Claims

- Do not claim a final constrained learned UAV slow controller is implemented.
- Do not claim learned fast-layer dominance on real LSS-HSR-L before real-data and remote multi-seed evidence.
- Do not claim one monolithic borrowed RL algorithm is the contribution.

## Next Algorithm Steps

- Keep current submission narrative scoped to architecture + role-split QRS + fast-layer options.
- After real-data audit, train/calibrate fast UE/query selector on radar-triggered questions.
- Only add constrained PPO/CMDP slow UAV learner after the calibrated fast layer and risk guard are stable.
