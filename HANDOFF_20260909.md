# 模型交接：HPPO-VQA-TGCN 论文与实验

更新时间：2026-09-09。这是研究状态交接，不是论文正式结果。请先读本文件，再读当前诊断和冻结迁移报告。不要从头重跑，不要以历史高分替换修正后的结果。

## 1. 当前用户目标与接手优先级

用户正在修改面向 TGCN/TCOM 等通信期刊的视觉问答通信论文。经过多轮检查，工作重点从文字修改转向实验公平性、任务真值、视觉依赖和系统准确率。用户关心：整体准确率不到60%，系统是否有价值、应该改哪里。

最近已授权一个子代理执行**无通信损伤任务能力与误差来源诊断**，暂缓继续改路由。当前用户要求交接给其他模型，未要求中断正在运行的诊断。

接手第一件事：查看 `clean_accuracy_error_diagnosis` 是否完成，核对原图与压缩图对照，读最终报告后向用户解释实际瓶颈。不要重复启动该实验。然后根据证据建议最小下一步，不自动训练新检测器/换VLM/改任务。

## 2. 位置、运行与用户偏好

- 本地仓库：`/Users/zhangqiankun/Documents/mpu/phd_research/HPPO-VQA-TGCN`
- 远端：`ssh lab-s2`
- 远端实验根：`/home/qiankun/phd_research/vqa_semcom`
- 常用解释器：`outputs/literature_baselines_20260908/env/bin/python`（相对远端根）
- 远端单 RTX4060；当前可能被原图Qwen诊断占用。先核查进程/GPU，不抢占、不终止其他任务。
- 本地主要论文：`paper/main.tex`、`paper/sections/`；中文：`paper/sections_zh/`。近几轮仅生成实验工件，**未将新结果写入论文**。
- 用户用中文交流；英文写作要求学术化、句子清楚，不用生僻词。通信论文叙事要合理，不能为精简牺牲必要解释。
- 用户多次明确要求子代理；仅在明确授权/适用指令允许时创建。当前不要因交接重复创建同一任务。
- 保留既有数据、检查点、旧结果。独立目录写新版本；文件编辑用 apply_patch。不根据目标结果反复调参再声称零适配。
- 用户不喜欢无意义重跑、线性与MLP几乎相同却反复强调线性、以及 S1/S2 这类分支称呼。

## 3. 正在运行的诊断（交接时尚未最终完成）

子代理名称：`clean_accuracy_error_diagnosis`（当前团队路径 `/root/clean_accuracy_error_diagnosis`，换模型后未必仍可访问团队句柄，优先看文件/远端进程）。

工件：`paper/outputs/clean_accuracy_error_diagnosis_20260909/`，远端同名 outputs 目录。

范围：源评估104图/1872候选问、目标256图/4608候选问，共6480问。规则、检测、已有JPEG与基线clean参考、通信损失分解完整执行；仅缺失的Qwen原图需要新推理。

原图对照：首5问约0.947秒/问，全量预计102分钟，故按预声明45分钟GPU上限，**在看答案前SHA固定140图/2520问，每图全部18问，源目标同比例**。不要把这份子集结果与全360图结果直接相减。后续约0.6秒/问，最近进度400/2520，剩余约20–25分钟（此为历史更新，接手必须查实时状态）。

已回报初步结果（待最终报告验收，不冒称原图推理已完成）：

- 理想标注证据：源1872/目标4608问规则与真值100%一致，配对也100%；343个规则边界与21个计数容差断言通过。
- 冻结源计数倍率 car/bus/van 均为1.0，所以当前校准与不校准参考相同。
- 真实检测无损配对：源65.62%、目标64.77%；通信后57.77%/56.16%。目标链路净降低8.61个百分点，无损时仍有35.23%错误，不能归咎单一信道。
- Qwen固定JPEG无损配对：源52.14%、目标56.52%。RSVQA clean55.79%/46.53%，T24 noiseless61.71%/50.00%。这些是固定模型参考，不是真正理论上限。
- 目标计数无损：检测55.63%，Qwen JPEG30.31%。
- 检测框诊断：目标预测van的280框中，类无关IoU匹配170个对应GT car、24个对应GT van；源205个小van，同类IoU≥0.5仅匹配8个。
- 框诊断使用目标多边形水平包围框，不是官方AP；人工核查两例坐标，保留目标白边，没有发现整体100像素错位。
- 检查过VisDrone score过滤和目标 difficult：当前公共类别没有实际受影响条目，未直接改正式真值。

交接写入后的最新进度：原图推理约800/2520；39个冻结文件SHA、360个源JPEG SHA、6480条Qwen缓存键/提示/配置已核验一致。高SNR检测配对源约65.1–65.7%、目标63.6–64.8%，低SNR拉低整体但不是唯一瓶颈。

当前目录已有 `analysis-report.md`、`protocol.md`、`clean_summary.csv`、`communication_decomposition.csv`、`count_bias.csv`、框匹配表、`original_vlm.py`、`finalize.py`、核验文件。**文件存在不意味着原图结果/最终报告完成**。

## 4. 系统与正确比较口径

本文：冻结 YOLOv8n（此前在VisDrone训练过）产生检测证据；结构化符号规则回答；另一路冻结 Qwen2-VL-2B回答JPEG；源域拟合计数倍率和小型双BCE MLP选择分支。

“冻结”指本轮/部署不更新检测器和VLM，不代表它们未用领域数据。新环境可全部冻结；若更新校准/路由是轻量适配，不能称零适配。

基线：RSVQA与T-DeepSC为当前任务适配版本，非原论文准确率的严格复现。RSVQA使用冻结视觉/语言前端及训练任务头；T24传学习复符号。两基线同样冻结后迁移，不能说只有本文无需目标训练。

当前共同信道：单位平均复符号功率，`y=h*x+n`，Rician K=6dB，每图/seed块衰落，完美CSI，复噪声方差 `10^(-EsN0/10)`；SNR为−5/0/5/10/15/20dB，3信道seed1701–1703，跨SNR配对。

数字：LDPC(1020,512)、QPSK、48源字节/码块、8B头+4B CRC+填充，每块510复符号，分包共享h、无重传、全包CRC恢复。失败统一用源训练问题先验。图像与RSVQA必须使用同一接收字节，旧正确性不能套新图。

T24实际24复符号，没有CRC，不能造“交付率”。共同最大预算524288符号**不是相同实际资源**，不可补零伪装相同；资源数字不等于完整系统能耗。

## 5. 重要历史修正，不能回退

1. 旧JPEG实际走理想遍历速率预算+outage，不是声称的完整LDPC；outage只按1500B门限，与实际JPEG大小不一致。检测独立逐记录损失也不等于物理包传输。现共同PHY替代这些抽象。
2. 旧检测包损失以256B计算，实际约943B；只能说损失概率输入不一致，不能说旧全部能耗被低估3.68倍。
3. 旧问题先验强：训练问题多数查表验证76.64%，打乱视觉准确率几乎不变。
4. 旧生成器计数类遗漏bicycle，共现生成器将无计数行当不存在，103训练+35验证题真值错误。新任务直接读取全类别原标注。
5. 当前任务按固定源问题库跨图求真值，再配对相反二元答案/不相交计数容差。完整候选与配对都必须报告，不能挑高分口径。
6. 旧548图全来自官方DET val；现用官方train池训练。图像字节去重不等于视频/场景独立，历史val也参与检测器选择。
7. DroneVehicle旧freight car→truck映射不可靠。当前公共类别只car/bus/van，RGB valimg/vallabel，不混IR。部分XML depth写错但实际彩色，已审计保留。

## 6. 最近完整源训练与冻结迁移结果

证据主目录：`paper/outputs/source_training_frozen_transfer_20260909/`。

- source train1024图/18432候选/8632配对问；val66图/1188候选/526配对。
- source_eval104图/1872候选/794配对；target256图/4608候选/1212配对。
- 18源域问题，公共car/bus/van。T24/RSVQA各24epoch、单训练seed；3信道seed不是3次训练。
- 检测器/Qwen冻结；2306参数MLP、校准与价格只源train/val拟合，目标前SHA冻结。目标更新0。
- 所有既定计算完成约2小时17分。源Qwen证据19620次57分钟；不能只报MLP76秒而隐瞒证据成本。
- 26100标签、1450图SHA、所有方法题/SNR/seed集合匹配通过。
- 目标84/256身份与历史任务有交集，其余不能宣称完全未曝光。目标现已用于多轮诊断，后续只能称开发参考，需新集合确认泛化。

| 配对集/六SNR三seed平均 | 源准确率 | 目标准确率 | 目标复符号/问 |
|---|---:|---:|---:|
| 问题先验 |42.44%|41.42%|0|
| 固定检测 |57.77%|56.16%|9066|
| 固定图像Qwen |48.75%|50.83%|161930|
| 旧规则 |54.77%|51.36%|26502|
| 原准确率MLP |57.64%|56.41%|36184|
| 资源预算价格MLP |57.65%|56.06%|20853|
| RSVQA |51.05%|44.56%|161930|
| T24 |60.50%|49.95%|24|

近检测精度最低成本选择完全等于固定检测。17共同问题等权：检测63.08→59.31，T24 65.32→54.20，RSVQA55.49→49.76。支持当前检测路径迁移保留性，非普遍免适配保证。目标完整候选检测77.85%，先验69.94%，不能只报高分。

## 7. 路由审计与已完成小消融

`paper/outputs/routing_error_complementarity_audit_20260909/analysis-report.md`：

- 目标21816事务，图像独自正确1503；原MLP救回126、损害70、漏选1377，净56=0.2567pp。
- 图像调用3918中95%两分支正确性相同；77.82%调用时两链路均失败，浪费76.99%额外符号。事后CRC不能做发送前门控。
- 98.27%潜在救回发生在两边成功交付后，主要是语义互补，不是独立信道备份。
- 原18维遗漏第二类别/计数/置信度/问句阈值；事后oracle63.05不可部署，不要当可实现收益。

`paper/outputs/small_router_ablation_20260909/analysis-report.md`：

- 3训练seed(0/1/2)，原18维/增强39维×原选择/成本选择，保持双BCE隐藏32/16；CPU108.65秒，0大模型调用。
- 39维加第二类别、数量关系、置信mean/min/max/mask、阈值/mask、发送符号数，无接收CRC/答案泄漏。
- margin/price只源val选，主准则“固定检测−0.5pp内最低成本”全部选择固定检测。
- 增强原选择源58.36%/48940符号、目标56.97%/40811；增强25k源58.14%/20024、目标56.61%/19033。
- 对固定检测收益仅约0.37/0.46pp、符号约翻倍；对旧MLP可省约一半。目标完整候选仍负增益，源差区间跨0。
- 不建议继续无界调路由。用户随后关注整体低准确率，已转向当前无损诊断，**不要错误地重启新源批次路由确认**。

## 8. 接手操作建议与汇报原则

1. 查看当前诊断报告/子代理/远端进程，等待或接续原图推理和finalize；不要重复运行。
2. 核验原图比较是同一140图/2520问子集、processor分辨率是否相同；原文件分辨率不等于VLM真正输入分辨率。
3. 完整规则/真实检测/固定JPEG诊断用全360图；与子集原图比较严格分开。
4. 核验通信恒等式 `A=Psucc*A_success+(1-Psucc)*A_fail`；交付成功条件准确率有选择偏差，不等于全数据无损准确率。
5. 给用户结论：理想规则是否正确；无损检测上限（参考）与VLM能力；原图能否改善压缩后表现；低SNR造成多少净损失；然后建议改哪一环。
6. 未经新的明确任务授权，不训练新检测器、不调target校准、不更换VLM、不修改论文宣称优势。当前授权允许有界原图补推理诊断。
7. 所有图表需明确数据集/分母/题型口径/训练seed与信道seed/成功条件。低于60%不自动无用，高于先验也不自动可靠部署。

可直接给下一模型的开场任务：

> 请接续 HANDOFF_20260909.md 中的工作。先检查 clean_accuracy_error_diagnosis 子代理及远端原图推理，不重复启动。核对最终无损能力和误差分解报告，尤其同子集原图/压缩图比较，向我解释整体准确率低的主要瓶颈与最小下一步。保持检测器/VLM/源校准和目标域策略冻结，不修改论文正式结果。
